[pytest]
markers =
    integration: marks tests as integration tests (require API credentials)
    slow: marks tests as slow (>1s execution time)
filterwarnings =
    ignore::ResourceWarning:unittest.mock
    ignore::UserWarning:src.config
